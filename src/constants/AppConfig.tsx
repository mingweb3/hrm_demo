export const AppConfig = {
  site_name: 'PaveHrm',
  title: 'pavegroup.com',
  description: 'PAVE HRM',
  locale: 'en'
}

// API
export const { VITE_BASE_API } = import.meta.env

// DEFAULT ROUTE
export const DEFAULT_URL = '/admin/dashboard'
